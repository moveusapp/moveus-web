query GetEvent($eventId: Int!) {
  event(id: $eventId) {
    ...Event
  }
}

query GetAnonymousUserEvents {
  anonymousUserEvents {
    ...EventCard
  }
}

query GetUnratedEvents {
  unratedEvents {
    id
    title
    endTime
    activity {
      id
    }
  }
}
