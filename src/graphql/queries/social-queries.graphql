query GetMyFollowers {
  followers {
    timeCreated
    user {
      ...UserCard
    }
  }
}

query GetMyFollowing {
  following {
    timeCreated
    user {
      ...UserCard
    }
  }
}

query GetPost($id: Int!) {
  post(id: $id) {
    ...PostCard
  }
}

query GetFeed {
  joinedEvents {
    posts {
      ...PostCard
    }
    organizer {
      user {
        id
        username
        firstName
        lastName
      }
    }
  }
}
