query GetFeed {
  joinedEvents {
    posts {
      ...Post
    }
    organizer {
      user {
        id
        username
        firstName
        lastName
      }
    }
  }
}

query GetHomeEvents {
  futureJoinedEvents {
    ...EventCard
  }
  ongoingJoinedEvents {
    ...EventCard
  }
}