query GetMyProfile {
    myProfile {
        ...ContextProfile
    }
}

query GetAvatarUpload($contentType: String!) {
  avatarUpload(contentType: $contentType) {
    ...UploadTicket
  }
}

query GetNotifications {
  myNotifications(lastFetch: "1971-01-01") {
    id
    time
    notificationType
    ... on UserNotificationType {
      user {
        id
        username
        firstName
        lastName
        avatarUrl
      }
    }
    ... on EventNotificationType {
      event {
        id
        title
      }
    }
  }
}
