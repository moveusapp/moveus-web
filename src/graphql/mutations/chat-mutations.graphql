mutation SendChatMessage (
  $chatId: Int!,
  $message: String,
  $attachmentId: String
) {
  sendChatMessage (
    chatId: $chatId,
    message: $message,
    attachmentId: $attachmentId
  ) {
    chatMessage {
      ...ChatMessage
    }
  }
}